name: k8s deployment  
on: [push]
jobs:
  build-frontend:
      runs-on: ubuntu:latest
      steps:
        - uses: actions/checkout@v2
        - name: Set up Node.js
          uses: actions/setup-node@v2
        - run: |
              cd frontend &&
              npm install &&
              npm run build &&
              ls -la